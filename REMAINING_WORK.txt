============================================================
  PETAL PLATFORM — REMAINING WORK (UPDATED 2026-03-22)
  Previously: 20 issues | Now: 6 remaining
============================================================

──────────────────────────────────────────────────────────
 ✅ FIXED IN THIS SESSION
──────────────────────────────────────────────────────────

[A1] ✅ Dashboard mood logger — Now calls POST /api/period-tracker/mood-today
     and refreshes summary stats after successful save.

[A2] ✅ Community "My Posts" filter — Now fetches from API using
     GET /api/community/?user_id=<id>. Works correctly after page refresh.

[A3] ✅ Chatbot page created — Full UI at /cycle-tracker/chatbot:
     calls POST /api/chatbot/message with message, history, and language.
     Added to sidebar nav as "AI Assistant". Language selector included.

[B1] ✅ Cycle day stat — Now computes real cycle day from last log start date
     instead of showing today's calendar date.

[B2] ✅ Risk Analysis date filters — Now re-fetch data from
     GET /reports/risk-analysis?days=N when the filter is changed.

[B3] ✅ Hardcoded % changes on Risk Analysis — Removed fake "↓5%" and "↗12%"
     strings from KPI cards.

[B4] ✅ Insights empty state — Replaced blank page with a friendly empty
     state card that prompts the user to log data.

[B5] ✅ Non-functional search bar on Dashboard — Removed entirely.

[B6] ✅ Community avatar color randomization — Color is now derived
     deterministically from post ID using a hash function.

[B7] ✅ Profile logout UX — Already handled, navigates to '/'.

[B8] ✅ Broken /contact /privacy /terms links — Created stub pages for all three
     with real content relevant to the platform.

[D2] ✅ Report Generator — Print and CSV options now visually grayed out with
     "Soon" badge so users aren't misled.

[D3] ✅ Protected routes — Added ProtectedRoute wrapper in App.jsx.
     /profile and /cycle-tracker/* now redirect to /login if not authenticated.

[D4] ✅ CycleTracker save — Now shows error toast on failure instead of silent
     console.error.

[D5] ✅ Community flag — Now shows confirmation toast after flagging a post.
     Shows "already flagged" toast for 409 response.

[E2] ✅ Page transition jank — Removed PageWrapper from CycleTrackerLayout.
     The layout and sidebar no longer animate on route changes within
     /cycle-tracker. Only top-level route changes animate.

──────────────────────────────────────────────────────────
 ❌ STILL REMAINING (6 items)
──────────────────────────────────────────────────────────

[A4] Google OAuth end-to-end   
  - loginWithGoogle() exists but has not been tested against the real backend.
  - VITE_GOOGLE_CLIENT_ID must be set in the frontend .env file.
  - The callback redirects to /cycle-tracker — verify this works with backend.
  - Status: Partially implemented, untested.

[A5] Cycle Preferences not loading from backend on refresh
  - Settings page initializes from user?.cycle_preferences?.average_cycle_length
  - But only if GET /users/me (getCurrentUser) returns a nested cycle_preferences
    object. If that field is missing from the API response, sliders will always
    reset to defaults (28/5/14) after page refresh.
  - ACTION: Verify that backend /users/me response includes cycle_preferences.

[D1] Education page — Empty
  - Education.jsx exists but renders no content.
  - Will be populated once the Education API / Google integration is ready.
  - Status: Acknowledged / waiting on backend.

[D6] Mobile sidebar — No hamburger toggle
  - The /cycle-tracker sidebar is fixed-width (w-64) with no mobile collapse.
  - On narrow screens, the sidebar covers the full viewport.
  - FIX: Add a hamburger button to toggle sidebar visibility on small screens.

[D7] Notification delivery
  - Settings → Notifications saves toggle states to the backend.
  - But there is no actual notification delivery mechanism in the frontend
    (no push, no bell dropdown in the header).
  - This is a future feature / infra task.

[E4] Login/Register form — No inline validation
  - Only HTML5 `required` attributes are present.
  - No password strength indicator, no email format check, no age range
    validation with visible error messages.
  - FIX: Add client-side validation with inline error messages.

──────────────────────────────────────────────────────────
 PRIORITY ORDER (Remaining)
──────────────────────────────────────────────────────────

  🔴 CRITICAL:
    D6 (mobile sidebar — site is unusable on phones)

  🟡 IMPORTANT:
    A4 (Google OAuth verification)
    A5 (cycle preferences sync verification)
    E4 (form validation)

  🟢 FUTURE / INFRA:
    D1 (Education page — awaiting backend)
    D7 (Notification delivery)

============================================================
  END OF AUDIT
============================================================
